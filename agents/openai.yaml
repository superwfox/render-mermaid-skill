interface:
  display_name: "Mermaid SVG/PNG"
  short_description: "将 MMD 忠实绘制为 ClaudeLike SVG 与 2× PNG"
  default_prompt: "Use $render-mermaid to render this MMD as a ClaudeLike SVG and transparent 2× PNG."
